const initialState = {
    username: '',
    email: '',
    password: '',
    user_id: '',

}

export default (state = initialState, action) => {
    const {type, payload} = action
        switch (type) {
            default : return state
        }
    }
